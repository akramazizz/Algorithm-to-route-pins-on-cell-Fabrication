To run the program, adjust the name of the input file in the main.cpp the main() function to the desired input file. 

Then open the terminal and write the command "make". that would run the program on your behalf